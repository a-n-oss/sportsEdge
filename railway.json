{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watch": ["apps/api/**"]
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host '::' --port 8000",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
