{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watch": ["**"],
    "nixpacksPlan": {
      "providers": ["python"],
      "variables": {
        "NIXPACKS_PYTHON_PACKAGE_MANAGER": "uv"
      },
      "phases": {
        "install": {
          "cmds": ["if [ -d apps/api ]; then cd apps/api; fi; uv pip install -r requirements.lock"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "if [ -d apps/api ]; then cd apps/api; fi; python -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/v1/leagues",
    "healthcheckTimeout": 100
  }
}
