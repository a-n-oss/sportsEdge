{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watch": ["apps/api/**", "requirements.lock"],
    "buildCommand": "cd apps/api && uv pip install -r requirements.lock"
  },
  "deploy": {
    "startCommand": "cd apps/api && uv run uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/v1/leagues",
    "healthcheckTimeout": 100
  }
}
